(()=>{
"use strict";
const SNAPSHOT="https://raw.githubusercontent.com/umarvandutch/Gold-Mine/main/live-data.json";
const TV="https://s3.tradingview.com/external-embedding/embed-widget-advanced-chart.js";
const W={rates:45,inflation:30,labour:24,growth:18,consumer:12,business:10,trade:8,housing:7,government:6,other:5};
const CHECK_KEY="goldmine-last-check-at",SNAPSHOT_KEY="goldmine-last-snapshot-at";
let data=null,pollTimer=null,chartTimer=null,chartState="idle";
const clamp=(n,a,b)=>Math.max(a,Math.min(b,n));
const num=v=>{if(v===null||v===undefined||v==="")return null;const n=Number(String(v).replace(/,/g,""));return Number.isFinite(n)?n:null};
const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const active=()=>document.getElementById("view-predictions")?.classList.contains("active");
function ts(v){if(!v)return null;const n=new Date(v).getTime();return Number.isFinite(n)?n:null}
function ageH(v){const n=ts(v);return n===null?99999:Math.max(0,(Date.now()-n)/36e5)}
function category(x){if(x?.category&&W[x.category]!==undefined)return x.category;const s=String(x?.name||x?.title||"").toLowerCase();if(/fomc|interest rate|fed funds|federal reserve|powell|fed minutes|fed meeting|fed rate|jackson hole/.test(s))return"rates";if(/cpi|pce|ppi|inflation|price index/.test(s))return"inflation";if(/payroll|employment|unemployment|jobless|jolts|job openings|wage|earnings/.test(s))return"labour";if(/gdp|gross domestic product/.test(s))return"growth";if(/retail|consumer confidence|consumer sentiment|personal spending|personal income/.test(s))return"consumer";if(/housing|home sales|building permits|mortgage|construction|housing starts/.test(s))return"housing";if(/pmi|ism|industrial production|factory|durable|manufacturing|services/.test(s))return"business";if(/trade|export|import|current account/.test(s))return"trade";if(/treasury|auction|budget|government/.test(s))return"government";return"other"}
function longMemory(s,c){return /\bcpi\b|consumer price index/i.test(String(s||""))||c==="rates"||/\bfomc\b|federal reserve|interest rate|fed funds|rate decision|rate cut|rate hike|powell|fed minutes|hawkish|dovish|higher for longer/i.test(String(s||""))}
function segment(h,long){if(h<=48)return"fresh";if(h<=168)return"weekly";if(long&&h<=2160)return"regime";return null}
function decay(h,seg){if(seg==="fresh")return Math.pow(.5,h/24);if(seg==="weekly")return .72*Math.pow(.5,(h-48)/72);if(seg==="regime")return .28*Math.pow(.5,((h-168)/24)/28);return 0}
const PROFILE=[[/core.*cpi|cpi.*core/i,.15,1.22,false],[/\bcpi\b|consumer price index/i,.20,1.18,false],[/core.*pce|pce.*core/i,.15,1.18,false],[/\bpce\b.*price/i,.18,1.14,false],[/\bppi\b|producer price/i,.25,1,false],[/non.?farm payroll|payrolls/i,75,1.18,false],[/unemployment rate/i,.15,1.14,true],[/average hourly earnings|wage growth/i,.18,1.08,false],[/initial jobless claims/i,15,.92,true],[/continuing jobless claims/i,35,.74,true],[/\bgdp\b|gross domestic product/i,.5,1.06,false],[/retail sales/i,.4,1.02,false],[/\bism\b/i,1.5,1.06,false],[/\bpmi\b/i,1.6,.94,false],[/interest rate decision|fed funds rate|fomc.*rate/i,.25,1.35,false]];
function profile(name,cons){for(const [re,scale,mult,inverse] of PROFILE)if(re.test(name))return{scale,mult,inverse};const c=Math.abs(cons||0);return{scale:Math.max(c*.05,c<2?.15:1),mult:1,inverse:/unemployment|jobless|trade deficit|layoffs/i.test(name)}}
function sourceFactor(e){const st=e?.officialVerification?.status;if(st==="mismatch")return.42;if(st==="matched"||st==="official-filled")return 1.12;if(e?.primarySource)return 1.08;return 1}
function eventEvidence(e){if(!e?.dateUtc||!["MEDIUM","HIGH"].includes(String(e.volatility||"").toUpperCase())||e.isSpeech)return null;const a=num(e.actual),c=num(e.consensus);if(a===null||c===null)return null;const cat=category(e),name=String(e.name||"US release"),h=ageH(e.dateUtc),seg=segment(h,longMemory(name,cat));if(!seg)return null;const p=profile(name,c);let z=(a-c)/p.scale;if(p.inverse)z*=-1;let usd=Math.tanh(z*.78);const prev=num(e.previous),rev=num(e.revised);if(prev!==null&&rev!==null&&rev!==prev){let rz=(rev-prev)/p.scale;if(p.inverse)rz*=-1;usd=(usd+.28*Math.tanh(rz*.72))/1.28}const vol=String(e.volatility).toUpperCase()==="HIGH"?1:.62;const score=-usd*(W[cat]||5)*vol*p.mult*decay(h,seg)*sourceFactor(e);if(Math.abs(score)<.12)return null;return{type:"release",category:cat,segment:seg,score,name,age:h,detail:`Actual ${a} vs ${c} consensus · ${String(e.volatility).toUpperCase()}${e?.officialVerification?.status==="matched"?" · official matched":e?.officialVerification?.status==="mismatch"?" · official mismatch reduced":""}`}}
function headlineImpact(h){if(h?.impact)return String(h.impact).toLowerCase();const s=`${h?.title||""} ${h?.officialText||""}`.toLowerCase();if(longMemory(s,h?.category))return"high";return/non.?farm|payroll|unemployment|average hourly earnings|\bpce\b|\bppi\b|\bgdp\b|retail sales|\bism\b|\bpmi\b|jobless claims|jolts|tariff|sanction|war|attack|geopolit|recession|financial stress|banking stress/.test(s)?"medium":"low"}
function headlineDir(h){const s=`${h?.title||""} ${h?.officialText||""}`.toLowerCase();if(/higher for longer|hawkish|rate hike|rates?.*higher|no rush.*cut|delay.*rate cut|not ready.*cut|inflation.*stubborn|inflation.*elevated|inflation.*hot|inflation.*accelerat/.test(s))return[-1,"hawkish / hotter inflation",1.15];if(/dovish|rate cut|cut rates|easing|lower rates|disinflation|inflation.*cool|inflation.*ease|inflation.*slower/.test(s))return[1,"dovish / cooling inflation",1.1];if(/payroll|jobs|employment/.test(s)&&/(weak|slow|fall|miss|cool|declin)/.test(s))return[1,"labour weakness",.78];if(/payroll|jobs|employment/.test(s)&&/(strong|surge|beat|accelerat|robust)/.test(s))return[-1,"labour strength",.78];if(/war|attack|missile|military escalation|geopolit|sanctions escalation|crisis/.test(s))return[1,"safe-haven demand",.72];if(/recession|hard landing|financial stress|banking stress/.test(s))return[1,"risk-off / growth stress",.62];return null}
function headlineEvidence(h){if(!h?.title||headlineImpact(h)==="low")return null;const d=headlineDir(h);if(!d)return null;const cat=category(h),text=`${h.title} ${h.officialText||""}`,hr=ageH(h.publishedUtc),seg=segment(hr,longMemory(text,cat));if(!seg)return null;const official=!!h.primarySource||/Federal Reserve|Bureau of Labor Statistics|BLS|BEA|Census|Department of Labor|Treasury|EIA/i.test(String(h.source||""));const quality=official?1:.46,impact=headlineImpact(h)==="high"?1:.68,base=Math.min(13,(W[cat]||5)*.34);const score=d[0]*base*d[2]*quality*impact*decay(hr,seg);if(Math.abs(score)<.08)return null;return{type:"news",category:cat,segment:seg,score,name:h.title,age:hr,detail:`${d[1]} · ${headlineImpact(h).toUpperCase()}${official?" · official":" · aggregated"}`}}
function cluster(items){const groups=new Map(),out=[];for(const x of items){if(x.type!=="release"){out.push(x);continue}const k=`${x.segment}|${x.category}|${Math.round(x.age*12)}`;if(!groups.has(k))groups.set(k,[]);groups.get(k).push(x)}for(const g of groups.values()){if(g.length===1){out.push(g[0]);continue}const pos=g.filter(x=>x.score>0).reduce((s,x)=>s+x.score,0),neg=g.filter(x=>x.score<0).reduce((s,x)=>s+Math.abs(x.score),0),net=pos-neg,total=pos+neg,agr=total?Math.abs(net)/total:0,strong=[...g].sort((a,b)=>Math.abs(b.score)-Math.abs(a.score))[0],cap=Math.abs(strong.score)*(1+Math.min(.65,.22*Math.log2(g.length+1)));out.push({...strong,score:clamp(net,-cap,cap)*(.5+.5*agr),name:`${g.length} ${strong.category} releases together`,detail:`${g.slice(0,3).map(x=>x.name).join(" · ")}${agr<.45?" · conflict reduced":""}`})}return out}
function layer(items,cap,label){const by=new Map();for(const x of items){const k=x.category||x.type;if(!by.has(k))by.set(k,[]);by.get(k).push(x)}const cats=[];for(const [cat,g] of by){const p=g.filter(x=>x.score>0).reduce((s,x)=>s+x.score,0),n=g.filter(x=>x.score<0).reduce((s,x)=>s+Math.abs(x.score),0),tot=p+n,net=p-n,agr=tot?Math.abs(net)/tot:0;cats.push({cat,score:clamp(net*(.52+.48*agr),-cap*.58,cap*.58)})}const score=clamp(cats.reduce((s,x)=>s+x.score,0),-cap,cap),pos=cats.filter(x=>x.score>0).reduce((s,x)=>s+x.score,0),neg=cats.filter(x=>x.score<0).reduce((s,x)=>s+Math.abs(x.score),0),tot=pos+neg;return{label,score,agreement:tot?Math.abs(score)/tot:0,items,status:items.length?score>3?"bullish":score<-3?"bearish":"mixed":"unavailable"}}
function marketLayer(){const m=data?.market||{},items=[];const add=(name,key,score,detail)=>{if(Math.abs(score)>.05)items.push({type:"market",category:key,segment:"market",score,name,age:0,detail})};if(m.dxy&&Number.isFinite(num(m.dxy.changePct))){const v=num(m.dxy.changePct);add("US Dollar Index","dxy",-clamp(v/.55,-1,1)*7,`${v>0?"+":""}${v.toFixed(2)}%`)}if(m.real10y&&Number.isFinite(num(m.real10y.deltaBps))){const v=num(m.real10y.deltaBps);add("US real 10Y yield","real10y",-clamp(v/9,-1,1)*6,`${v>0?"+":""}${v.toFixed(1)} bps`)}if(m.us2y&&Number.isFinite(num(m.us2y.deltaBps))){const v=num(m.us2y.deltaBps);add("US 2Y yield","us2y",-clamp(v/11,-1,1)*4,`${v>0?"+":""}${v.toFixed(1)} bps`)}if(m.us10y&&Number.isFinite(num(m.us10y.deltaBps))){const v=num(m.us10y.deltaBps);add("US 10Y yield","us10y",-clamp(v/11,-1,1)*3,`${v>0?"+":""}${v.toFixed(1)} bps`)}if(m.xau&&Number.isFinite(num(m.xau.changePct))){const v=num(m.xau.changePct);add("XAUUSD price confirmation","xau",clamp(v/.7,-1,1)*5,`${v>0?"+":""}${v.toFixed(2)}%`)}const score=clamp(items.reduce((s,x)=>s+x.score,0),-18,18),mag=items.reduce((s,x)=>s+Math.abs(x.score),0);return{label:"USD / yields / XAU",score,agreement:mag?Math.abs(score)/mag:0,items,status:items.length?score>2.5?"bullish":score<-2.5?"bearish":"mixed":"unavailable"}}
function technicalLayer(){const t=data?.technical||{},items=[],h4=t?.structure4h?.state||"unavailable",d1=t?.structure1d?.state||"unavailable";if(h4==="bullish")items.push({score:8,name:"4H market structure",detail:"higher swing structure"});else if(h4==="bearish")items.push({score:-8,name:"4H market structure",detail:"lower swing structure"});if(d1==="bullish")items.push({score:4,name:"Daily structure",detail:"daily context bullish"});else if(d1==="bearish")items.push({score:-4,name:"Daily structure",detail:"daily context bearish"});const bull=t.preferredBullishOrderBlock,bear=t.preferredBearishOrderBlock;if(bull&&bull.quality>=64)items.push({score:Math.min(8,bull.quality/12),name:"Bullish 4H order block",detail:`${bull.status} · quality ${bull.quality}/100`});if(bear&&bear.quality>=64)items.push({score:-Math.min(8,bear.quality/12),name:"Bearish 4H order block",detail:`${bear.status} · quality ${bear.quality}/100`});const score=clamp(items.reduce((s,x)=>s+x.score,0),-20,20),mag=items.reduce((s,x)=>s+Math.abs(x.score),0);return{label:"4H technical",score,agreement:mag?Math.abs(score)/mag:0,items,status:t.status==="live"?(score>3?"bullish":score<-3?"bearish":"mixed"):"unavailable"}}
function nextEvent(){const now=Date.now();return(data?.events||[]).filter(e=>["MEDIUM","HIGH"].includes(String(e.volatility||"").toUpperCase())&&ts(e.dateUtc)>now).sort((a,b)=>ts(a.dateUtc)-ts(b.dateUtc))[0]||null}
function preferredOB(direction){const t=data?.technical||{};return direction==="bullish"?t.preferredBullishOrderBlock:direction==="bearish"?t.preferredBearishOrderBlock:null}
function compute(){const ev=cluster([...(data?.events||[]).map(eventEvidence).filter(Boolean),...(data?.headlines||[]).map(headlineEvidence).filter(Boolean)]),fresh=layer(ev.filter(x=>x.segment==="fresh"),42,"Fresh catalyst"),weekly=layer(ev.filter(x=>x.segment==="weekly"),24,"7-day macro"),regime=layer(ev.filter(x=>x.segment==="regime"),18,"90-day CPI/Fed"),market=marketLayer(),technical=technicalLayer();const macroScore=fresh.score+weekly.score+regime.score+market.score,macroMag=[fresh,weekly,regime,market].reduce((s,x)=>s+Math.abs(x.score),0),macroAgreement=macroMag?Math.abs(macroScore)/macroMag:0;let direction=Math.abs(macroScore)>=7?(macroScore>0?"bullish":"bearish"):"mixed";const next=nextEvent(),mins=next?Math.round((ts(next.dateUtc)-Date.now())/60000):null,eventBlock=mins!==null&&mins>=0&&mins<=90,technicalLive=data?.technical?.status==="live",ob=preferredOB(direction),sign=direction==="bullish"?1:direction==="bearish"?-1:0;const independent=[fresh,weekly,regime,market].filter(x=>sign&&Math.sign(x.score)===sign&&Math.abs(x.score)>=3).length;const opposed=[fresh,weekly,regime,market].filter(x=>sign&&Math.sign(x.score)===-sign&&Math.abs(x.score)>=4).length;const techAligned=sign&&Math.sign(technical.score)===sign&&Math.abs(technical.score)>=4;const techOpposed=sign&&Math.sign(technical.score)===-sign&&Math.abs(technical.score)>=6;const marketStrongOppose=sign&&Math.sign(market.score)===-sign&&Math.abs(market.score)>=5;const stale=data?.generatedAt?ageH(data.generatedAt)>1.5:true;let conviction=Math.round(clamp((Math.min(1,Math.abs(macroScore)/30)*46)+(macroAgreement*24)+(independent*7)+(techAligned?12:0)-(opposed*8)-(techOpposed?14:0)-(marketStrongOppose?10:0)-(stale?14:0),0,100));const blockers=[];if(direction==="mixed")blockers.push("Macro/news layers do not agree enough for a directional call.");if(eventBlock)blockers.push(`Medium/high-impact US event in ${mins} min.`);if(stale)blockers.push("Live snapshot is stale.");if(!technicalLive)blockers.push("Programmatic 4H XAU candles are not connected yet.");if(techOpposed)blockers.push("4H technical structure materially opposes the macro direction.");if(marketStrongOppose)blockers.push("USD/yield/XAU confirmation materially opposes the macro direction.");if(direction!=="mixed"&&independent<2)blockers.push("Fewer than two independent macro layers support the direction.");if(direction!=="mixed"&&technicalLive&&!ob)blockers.push(`No valid ${direction} 4H order block is currently available.`);if(ob&&!ob?.planning?.passesRewardRiskGate)blockers.push("The preferred order block does not currently pass the structure/reward-risk gate.");let filter="STAY OUT",stage="NO TRADE";if(direction!=="mixed"&&!eventBlock&&!stale&&independent>=2&&!marketStrongOppose&&!techOpposed){if(technicalLive&&ob&&ob?.planning?.passesRewardRiskGate){filter=direction==="bullish"?"LONGS ONLY":"SHORTS ONLY";if(ob.status==="in-zone")stage=direction==="bullish"?"LONG SETUP IN 4H BUY ZONE":"SHORT SETUP IN 4H SELL ZONE";else if(ob.status==="untouched")stage=direction==="bullish"?"WAIT FOR 4H BUY ZONE":"WAIT FOR 4H SELL ZONE";else stage=direction==="bullish"?"WATCH BULLISH 4H REACTION":"WATCH BEARISH 4H REACTION"}else if(!technicalLive){filter=direction==="bullish"?"LONG LEAN":"SHORT LEAN";stage="TECH FEED NEEDED BEFORE EXECUTION"}else{filter=direction==="bullish"?"LONG LEAN":"SHORT LEAN";stage="WAIT FOR A VALID 4H ORDER BLOCK"}}
return{direction,macroScore,macroAgreement,conviction,independent,layers:[fresh,weekly,regime,market,technical],market,technical,next,mins,blockers,filter,stage,ob,drivers:[...ev,...market.items].sort((a,b)=>Math.abs(b.score)-Math.abs(a.score)).slice(0,8)}}
function meta(dir){if(dir==="bullish")return{prediction:"GOLD UP ↑",cls:"long",plain:"Macro/news evidence currently favours higher XAUUSD."};if(dir==="bearish")return{prediction:"GOLD DOWN ↓",cls:"short",plain:"Macro/news evidence currently favours lower XAUUSD."};return{prediction:"MIXED •",cls:"wait",plain:"There is no clean directional edge right now."}}
function layerCard(l){const cls=l.status==="bullish"?"good":l.status==="bearish"?"bad":l.status==="mixed"?"mixed":"off";const txt=l.status==="bullish"?"GOLD UP":l.status==="bearish"?"GOLD DOWN":l.status==="mixed"?"MIXED":"NO DATA";return`<div class="gmv4-layer ${cls}"><span>${esc(l.label)}</span><strong>${txt}</strong><small>${l.items?.length||0} signals · ${l.score>=0?"+":""}${Number(l.score||0).toFixed(1)}</small></div>`}
function planning(ob,dir){if(!ob)return`<div class="gmv4-empty">No valid ${dir==="bullish"?"bullish":"bearish"} 4H order block is available.</div>`;const p=ob.planning||{},targets=p.targets||[];return`<div class="gmv4-ob ${dir==="bullish"?"long":"short"}"><div class="gmv4-obtop"><div><span>${dir==="bullish"?"BULLISH":"BEARISH"} 4H ORDER BLOCK</span><strong>${Number(ob.zoneLow).toFixed(2)} – ${Number(ob.zoneHigh).toFixed(2)}</strong><small>${esc(ob.status)} · quality ${ob.quality}/100 · ${ob.displacementATR} ATR displacement</small></div><b>${ob.quality}</b></div><div class="gmv4-levels"><div><span>Limit reference</span><strong>${p.limitReference??"—"}</strong><small>50% of candidate zone</small></div><div><span>Invalidation reference</span><strong>${p.invalidationReference??"—"}</strong><small>beyond distal edge + ATR buffer</small></div>${targets.map((x,i)=>`<div><span>TP${i+1} reference</span><strong>${x.price}</strong><small>${x.rMultiple}R to structure/liquidity</small></div>`).join("")}</div><div class="gmv4-confluence"><span class="${ob.fairValueGap?"on":""}">FVG ${ob.fairValueGap?"✓":"—"}</span><span class="${ob.liquiditySweep?"on":""}">Liquidity sweep ${ob.liquiditySweep?"✓":"—"}</span><span class="${ob.premiumDiscountAligned?"on":""}">Premium/discount ${ob.premiumDiscountAligned?"✓":"—"}</span><span class="${p.passesRewardRiskGate?"on":""}">R:R gate ${p.passesRewardRiskGate?"✓":"—"}</span></div><p class="gmv4-note">These are planning references derived from 4H structure, not instructions to place an order. Confirm the live chart before using any level.</p></div>`}
function driverRows(drivers){if(!drivers.length)return'<div class="gmv4-empty">No strong directional evidence is currently active.</div>';return drivers.map(d=>`<div class="gmv4-driver"><div><strong>${esc(d.name)}</strong><span>${d.score>0?"Supports GOLD UP":"Supports GOLD DOWN"} · ${esc(d.detail||"")}</span></div><b>${d.score>0?"+":"−"}${Math.abs(d.score).toFixed(1)}</b></div>`).join("")}
function ensureRoot(){const panel=document.getElementById("predictionPanel");if(!panel)return null;panel.style.display="none";document.getElementById("tradePlan4hView")?.remove();document.getElementById("tradeDecisionV3")?.remove();let root=document.getElementById("tradeDecisionV4");if(root)return root;root=document.createElement("div");root.id="tradeDecisionV4";root.className="gmv4-root";panel.parentElement?.insertBefore(root,panel);return root}
function render(){const root=ensureRoot();if(!root||!data)return;const r=compute(),m=meta(r.direction),t=data.technical||{},px=num(t.currentPrice)??num(data?.market?.xau?.price);root.innerHTML=`<section class="gmv4-hero ${m.cls}"><div class="gmv4-top"><div><span class="eyebrow">DECISION ENGINE V4 · 4H XAUUSD</span><h2>${m.prediction}</h2><p>${m.plain}</p></div><div class="gmv4-conv"><strong>${r.conviction}</strong><span>MODEL<br>CONVICTION</span></div></div><div class="gmv4-action"><div><span>TRADE FILTER</span><strong>${esc(r.filter)}</strong></div><div><span>EXECUTION STATUS</span><strong>${esc(r.stage)}</strong></div><div><span>XAUUSD</span><strong>${px!==null?px.toFixed(2):"NO LIVE PRICE"}</strong></div></div><small>Conviction measures agreement and data quality. It is not a win probability or profit guarantee.</small></section><section class="gmv4-card"><div class="gmv4-head"><div><span class="eyebrow">WHY THIS CALL</span><h3>Five independent layers</h3></div><span class="gmv4-pill">${r.independent} MACRO LAYERS ALIGNED</span></div><div class="gmv4-layers">${r.layers.map(layerCard).join("")}</div></section>${r.blockers.length?`<section class="gmv4-card gmv4-block"><span class="eyebrow">WHAT IS STOPPING A CLEANER TRADE</span><h3>Blockers / warnings</h3><ul>${r.blockers.map(x=>`<li>${esc(x)}</li>`).join("")}</ul></section>`:""}<section class="gmv4-card"><span class="eyebrow">4H ORDER-BLOCK PLAN</span><h3>${r.direction==="bullish"?"Buy-side planning zone":r.direction==="bearish"?"Sell-side planning zone":"Wait for direction first"}</h3>${r.direction==="mixed"?'<div class="gmv4-empty">No order-block execution plan is promoted while the macro prediction is mixed.</div>':planning(r.ob,r.direction)}</section><section class="gmv4-card"><div class="gmv4-head"><div><span class="eyebrow">4H EXECUTION CHART</span><h3>XAUUSD · 4 hour</h3></div><span class="gmv4-pill">4H</span></div><div id="gmv4Chart" class="gmv4-chart"><div class="gmv4-chartmsg">Open Trade Plan to load the 4H chart.</div></div><p class="gmv4-note">Use this to confirm the detected zone, reaction, displacement and structure before acting.</p></section><section class="gmv4-card"><span class="eyebrow">STRONGEST EVIDENCE</span><h3>What is moving the prediction</h3><div class="gmv4-drivers">${driverRows(r.drivers)}</div></section><section class="gmv4-card"><span class="eyebrow">INVALIDATION</span><h3>When to abandon the idea</h3><ul class="gmv4-list">${r.direction==="bullish"?'<li>A completed 4H candle closes through the bullish order block invalidation level.</li><li>Fresh US data flips the rates/USD direction against gold.</li><li>DXY/real yields strengthen enough to oppose the long thesis.</li>':r.direction==="bearish"?'<li>A completed 4H candle closes through the bearish order block invalidation level.</li><li>Fresh US data turns materially dovish/weak or safe-haven demand strengthens.</li><li>DXY/real yields weaken enough to oppose the short thesis.</li>':'<li>Wait until the macro layers and 4H structure stop contradicting each other.</li>'}</ul></section><section class="gmv4-card"><span class="eyebrow">NEXT CATALYST RISK</span><h3>${r.next?esc(r.next.name):"No upcoming medium/high-impact release loaded"}</h3><p>${r.next?`${new Date(r.next.dateUtc).toLocaleString()} · ${r.mins} min away`:"Continue monitoring the live feed."}</p></section>`;setTimeout(()=>initChart(),60);updateFreshness()}
function initChart(force=false){if(!active())return;const host=document.getElementById("gmv4Chart");if(!host||host.clientWidth<120)return;if(chartState==="loaded"&&!force)return;if(chartState==="loading"&&!force)return;clearTimeout(chartTimer);chartState="loading";host.innerHTML='<div class="tradingview-widget-container" style="height:100%;width:100%"><div class="tradingview-widget-container__widget" style="height:100%;width:100%"></div></div>';const wrap=host.querySelector(".tradingview-widget-container"),script=document.createElement("script");script.src=TV;script.async=true;script.type="text/javascript";script.textContent=JSON.stringify({autosize:true,symbol:"OANDA:XAUUSD",interval:"240",timezone:"Etc/UTC",theme:"light",style:"1",locale:"en",allow_symbol_change:false,hide_top_toolbar:false,hide_side_toolbar:false,withdateranges:true,calendar:false,support_host:"https://www.tradingview.com"});script.addEventListener("error",()=>chartFail("TradingView failed to load."),{once:true});wrap.appendChild(script);const started=Date.now();const watch=()=>{if(host.querySelector("iframe")){chartState="loaded";clearTimeout(chartTimer);return}if(Date.now()-started<12000&&chartState==="loading")setTimeout(watch,300)};watch();chartTimer=setTimeout(()=>{if(chartState==="loading")chartFail("4H chart timed out.")},12500)}
function chartFail(msg){chartState="failed";const h=document.getElementById("gmv4Chart");if(!h)return;h.innerHTML=`<div class="gmv4-chartmsg"><strong>${esc(msg)}</strong><button id="gmv4Retry" type="button">Retry chart</button></div>`;document.getElementById("gmv4Retry")?.addEventListener("click",()=>{chartState="idle";initChart(true)},{once:true})}
function injectStyle(){if(document.getElementById("gmv4-style"))return;const s=document.createElement("style");s.id="gmv4-style";s.textContent=`.gmv4-root{display:grid;gap:13px}.gmv4-hero,.gmv4-card{background:#fff;border:1px solid #e6e1d7;border-radius:18px;box-shadow:0 5px 20px rgba(30,35,42,.05)}.gmv4-hero{padding:18px;border-left:6px solid #a8863b}.gmv4-hero.long{border-left-color:#29724d}.gmv4-hero.short{border-left-color:#9e4842}.gmv4-card{padding:15px}.gmv4-top,.gmv4-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.gmv4-hero h2{font-size:30px;margin:4px 0}.gmv4-hero.long h2{color:#246b48}.gmv4-hero.short h2{color:#963f3a}.gmv4-hero p,.gmv4-card p{color:#656b74;line-height:1.45}.gmv4-conv{width:84px;height:84px;border-radius:50%;background:#f4f1e9;display:flex;flex-direction:column;align-items:center;justify-content:center;flex:0 0 auto}.gmv4-conv strong{font-size:25px}.gmv4-conv span{font-size:9px;text-align:center;letter-spacing:.06em}.gmv4-action{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:14px 0}.gmv4-action>div,.gmv4-layer,.gmv4-levels>div{background:#faf9f6;border-radius:12px;padding:10px}.gmv4-action span,.gmv4-levels span{display:block;font-size:9px;color:#777;text-transform:uppercase;letter-spacing:.07em}.gmv4-action strong,.gmv4-levels strong{display:block;margin-top:4px;font-size:13px}.gmv4-layers{display:grid;grid-template-columns:repeat(5,1fr);gap:7px;margin-top:10px}.gmv4-layer span{display:block;font-size:10px;color:#777}.gmv4-layer strong{display:block;font-size:12px;margin:4px 0}.gmv4-layer.good strong{color:#28714d}.gmv4-layer.bad strong{color:#9b4640}.gmv4-layer.mixed strong{color:#876a2f}.gmv4-layer.off{opacity:.65}.gmv4-pill{padding:5px 8px;border-radius:999px;background:#efeadd;font-size:9px;font-weight:800}.gmv4-block{border-color:#e0cfaa;background:#fffdf8}.gmv4-block li,.gmv4-list li{font-size:12px;line-height:1.45;margin:6px 0;color:#575e67}.gmv4-ob{border:1px solid #e7e2d8;border-radius:14px;padding:12px;margin-top:10px}.gmv4-ob.long{border-left:4px solid #29724d}.gmv4-ob.short{border-left:4px solid #9e4842}.gmv4-obtop{display:flex;justify-content:space-between;gap:10px}.gmv4-obtop span,.gmv4-obtop small{display:block;font-size:10px;color:#777}.gmv4-obtop strong{display:block;font-size:20px;margin:3px 0}.gmv4-obtop>b{font-size:22px}.gmv4-levels{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-top:10px}.gmv4-confluence{display:flex;flex-wrap:wrap;gap:6px;margin-top:10px}.gmv4-confluence span{font-size:9px;padding:5px 7px;border-radius:999px;background:#f1eee7;color:#888}.gmv4-confluence span.on{background:#e8f2ec;color:#2b6e4d}.gmv4-note{font-size:10px!important;color:#777!important}.gmv4-chart{height:430px;border-radius:14px;overflow:hidden;background:#f5f3ed;margin-top:10px}.gmv4-chartmsg{height:100%;display:flex;flex-direction:column;justify-content:center;align-items:center;gap:8px;color:#777;text-align:center;padding:20px}.gmv4-chartmsg button{border:0;border-radius:10px;padding:8px 12px}.gmv4-drivers{display:grid;gap:7px;margin-top:10px}.gmv4-driver{display:grid;grid-template-columns:1fr auto;gap:10px;background:#faf9f6;border-radius:11px;padding:9px}.gmv4-driver strong{display:block;font-size:11px}.gmv4-driver span{display:block;font-size:10px;color:#777;margin-top:3px}.gmv4-driver>b{font-size:10px}.gmv4-empty{padding:12px;border-radius:12px;background:#faf9f6;color:#747a82;font-size:12px}.eyebrow{font-size:9px;letter-spacing:.09em;text-transform:uppercase;color:#777}@media(max-width:700px){.gmv4-action{grid-template-columns:1fr}.gmv4-layers{grid-template-columns:1fr 1fr}.gmv4-levels{grid-template-columns:1fr 1fr}.gmv4-chart{height:380px}.gmv4-hero h2{font-size:27px}}`;document.head.appendChild(s)}
function rename(){const nav=document.querySelector('.nav-item[data-view="predictions"] span');if(nav)nav.textContent="Trade plan";const title=document.getElementById("prediction-title");if(title)title.textContent="Gold trade decision";const eye=title?.closest(".section-heading")?.querySelector(".eyebrow");if(eye)eye.textContent="4H XAUUSD · MACRO + TECHNICAL"}
function freshnessRow(){const last=document.getElementById("lastRefresh");if(!last)return;const row=last.closest(".build-info");if(row?.querySelector("span"))row.querySelector("span").textContent="Latest data snapshot";if(!document.getElementById("lastAppCheck")&&row?.parentElement){const x=document.createElement("div");x.className="build-info";x.innerHTML='<span>Last app check</span><strong id="lastAppCheck">—</strong>';row.parentElement.insertBefore(x,row)}}
function updateFreshness(){freshnessRow();const fmt=v=>{if(!v)return"—";const d=new Date(v),mins=Math.max(0,Math.floor((Date.now()-d.getTime())/60000));return`${d.toLocaleString()} (${mins<1?"just now":`${mins} min ago`})`};const a=document.getElementById("lastAppCheck");if(a)a.textContent=fmt(sessionStorage.getItem(CHECK_KEY));const s=document.getElementById("lastRefresh");if(s)s.textContent=fmt(data?.sourceQueriedAt||data?.generatedAt||sessionStorage.getItem(SNAPSHOT_KEY))}
async function fetchData(){const worker=String(window.GOLD_MINE_CONFIG?.liveWorkerUrl||"").trim();if(worker){try{const r=await fetch(`${worker}${worker.includes("?")?"&":"?"}t=${Date.now()}`,{cache:"no-store"});if(r.ok){const j=await r.json();if(Array.isArray(j.events))return j}}catch{}}const r=await fetch(`${SNAPSHOT}?t=${Date.now()}`,{cache:"no-store"});if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json()}
async function poll(){try{data=await fetchData();sessionStorage.setItem(CHECK_KEY,new Date().toISOString());const snap=data?.sourceQueriedAt||data?.generatedAt;if(snap)sessionStorage.setItem(SNAPSHOT_KEY,snap);render()}catch(e){console.debug("Decision Engine v4 refresh unavailable",e);updateFreshness()}finally{clearTimeout(pollTimer);pollTimer=setTimeout(poll,60000)}}
function boot(){injectStyle();rename();ensureRoot();const nav=document.querySelector('.nav-item[data-view="predictions"]');nav?.addEventListener("click",()=>setTimeout(()=>{rename();render();initChart()},120));document.addEventListener("visibilitychange",()=>{if(!document.hidden){clearTimeout(pollTimer);setTimeout(poll,250)}});setInterval(()=>{rename();updateFreshness()},5000);setTimeout(poll,100)}
boot();
})();
