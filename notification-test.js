(()=>{
"use strict";
function native(){return window.GoldMineNative&&typeof window.GoldMineNative.scheduleTestNotification==="function"?window.GoldMineNative:null}
function install(){const settings=document.querySelector('#view-settings .settings-card');if(!settings||document.getElementById('gmNotificationTest'))return;const card=document.createElement('div');card.id='gmNotificationTest';card.className='method-card';card.innerHTML='<span class="eyebrow">ANDROID ALERT TEST</span><h3>Test background notifications</h3><p id="gmNotifyTestText">Schedule a test notification, close Gold Mine, and the phone should alert you about one minute later.</p><div style="display:flex;gap:8px;flex-wrap:wrap"><button id="gmNotifyNow" class="text-button" type="button">Send test now</button><button id="gmNotifyLater" class="text-button" type="button">Test with app closed</button></div><p class="fine-print">The delayed test uses the same native Android alarm delivery path as scheduled economic-news warnings.</p>';settings.parentElement?.insertBefore(card,settings.nextSibling);const text=document.getElementById('gmNotifyTestText'),now=document.getElementById('gmNotifyNow'),later=document.getElementById('gmNotifyLater');const n=native();if(!n){text.textContent='Notification testing is available in the Android APK. The browser/PWA cannot call the native alert tester.';now.disabled=true;later.disabled=true;return}try{const s=n.notificationStatus();if(s==='permission-required')text.textContent='Allow Gold Mine notifications when Android asks, then tap a test button.';}catch{}
now.addEventListener('click',()=>{try{const r=n.sendTestNotificationNow();text.textContent=r==='sent'?'Test notification sent. Check your notification shade.':'Allow notification permission, then tap again.';}catch(e){text.textContent='Could not trigger the native test notification.'}});later.addEventListener('click',()=>{try{const r=n.scheduleTestNotification();if(r==='scheduled'){text.textContent='Test scheduled for about 1 minute from now. Close Gold Mine normally and lock your phone if you want — do not Force stop it.';later.textContent='Test scheduled ✓';setTimeout(()=>later.textContent='Test with app closed',65000)}else text.textContent='Allow notification permission, then tap again.';}catch(e){text.textContent='Could not schedule the background test notification.'}})}
const o=new MutationObserver(()=>install());o.observe(document.body,{childList:true,subtree:true});install();
})();
