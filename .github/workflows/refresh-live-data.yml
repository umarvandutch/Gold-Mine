name: Refresh Gold Mine live data

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'
  push:
    branches: [main]
    paths:
      - 'scripts/refresh_live_data.py'
      - 'scripts/enrich_official_data.py'
      - 'scripts/enrich_fed_minutes.py'
      - 'scripts/record_signal_history.py'
      - '.github/workflows/refresh-live-data.yml'

permissions:
  contents: write

concurrency:
  group: gold-mine-live-data
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: main
      - name: Refresh free calendar, news and market data
        run: python3 scripts/refresh_live_data.py
      - name: Cross-check official BLS data and enrich Fed text
        run: python3 scripts/enrich_official_data.py
      - name: Follow underlying FOMC minutes document
        run: python3 scripts/enrich_fed_minutes.py
      - name: Record model calibration history
        run: python3 scripts/record_signal_history.py
      - name: Commit data only when it changed
        run: |
          if git diff --quiet -- live-data.json data/signal-history.json; then
            echo "No material live-data or calibration-history change."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add live-data.json data/signal-history.json
          git commit -m "Refresh Gold Mine live data and calibration history"
          git push origin main
