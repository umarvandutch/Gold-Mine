(()=>{
"use strict";
const BACKUP="https://raw.githubusercontent.com/umarvandutch/Gold-Mine/main/live-data.json";
let latest=null,busy=false,timer=null;
const worker=()=>String(window.GOLD_MINE_CONFIG?.liveWorkerUrl||"").trim();
const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const n=v=>Number.isFinite(Number(v))?Number(v):null;
const val=(v,d=2)=>n(v)===null?"—":n(v).toFixed(d);
const ageMin=v=>{const t=Date.parse(v||"");return Number.isFinite(t)?Math.max(0,(Date.now()-t)/60000):null};
const ageText=v=>{const a=ageMin(v);return a===null?"Unavailable":a<1?"just now":a<60?`${Math.floor(a)} min ago`:`${Math.floor(a/60)}h ago`};
const fmt=v=>{const d=new Date(v||"");return Number.isNaN(d.getTime())?"—":d.toLocaleString()};
function active(){return document.getElementById("view-signals")?.classList.contains("active")}
function hasSignals(x){return !!x?.signals&&!!x.signals.buyLimit&&!!x.signals.sellLimit}
function install(){if(!document.getElementById("view-signals")){const s=document.createElement("section");s.id="view-signals";s.className="view";s.innerHTML='<div class="section-heading"><div><span class="eyebrow">STRICT 4H EXECUTION FILTER</span><h2>Signals</h2></div><button id="signalsRefresh" class="text-button">Refresh signals live</button></div><div id="signalsPanel"></div>';document.getElementById("view-settings")?.before(s)}if(!document.querySelector('.nav-item[data-view="signals"]')){const b=document.createElement("button");b.className="nav-item";b.dataset.view="signals";b.innerHTML='<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 17l5-5 4 3 7-8M17 7h3v3"/></svg><span>Signals</span>';document.querySelector('.nav-item[data-view="settings"]')?.before(b);b.addEventListener("click",show)}const nav=document.querySelector(".bottom-nav");if(nav)nav.style.gridTemplateColumns="repeat(5,1fr)";const rb=document.getElementById("signalsRefresh");if(rb&&!rb.dataset.v3){rb.dataset.v3="1";rb.addEventListener("click",()=>refresh(true))}}
function show(){document.querySelectorAll(".nav-item").forEach(x=>x.classList.toggle("active",x.dataset.view==="signals"));document.querySelectorAll(".view").forEach(x=>x.classList.toggle("active",x.id==="view-signals"));scrollTo(0,0);refresh(false)}
async function fetchData(force=false){if(!force&&hasSignals(window.GoldMineLiveSnapshot))return window.GoldMineLiveSnapshot;const w=worker();if(w){try{const r=await fetch(`${w}${w.includes("?")?"&":"?"}${force?"fresh=1&":""}signals=${Date.now()}`,{cache:"no-store",headers:{"Cache-Control":"no-cache"}});if(!r.ok)throw new Error(`Worker HTTP ${r.status}`);const j=await r.json();if(!hasSignals(j))throw new Error("Worker payload has no Signals");j._goldMineSource="worker";return j}catch(e){console.warn("Signals Worker request failed",e)}}const r=await fetch(`${BACKUP}?signalsBackup=${Date.now()}`,{cache:"no-store"});if(!r.ok)throw new Error(`Backup HTTP ${r.status}`);const j=await r.json();j._goldMineSource="github-backup";if(!hasSignals(j))throw new Error("Backup snapshot has no Signals");return j}
function normalized(key,side){const raw=latest?.signals?.[key]||{},blockers=Array.isArray(raw.blockers)?[...raw.blockers]:[],marketAt=raw.marketObservedAt||latest?.technical?.observedAt||latest?.market?.xau?.observedAt,generated=raw.generatedAt||latest?.signals?.generatedAt,ma=ageMin(marketAt),ga=ageMin(generated),validUntil=Date.parse(raw.validUntil||"");if(ma===null||ma>10)blockers.unshift(ma===null?"Market timestamp unavailable.":`Market data is ${Math.floor(ma)} minutes old.`);if(ga===null||ga>10)blockers.unshift(ga===null?"Signal timestamp unavailable.":`Signal calculation is ${Math.floor(ga)} minutes old.`);if(Number.isFinite(validUntil)&&Date.now()>validUntil)blockers.unshift("Signal validity window has expired.");const candidate=raw.status==="candidate"&&blockers.length===0;return{...raw,side,status:candidate?"candidate":"no-signal",blockers:[...new Set(blockers)],marketAt,generated}}
function card(s){const buy=s.side==="buy",go=s.status==="candidate",name=buy?"BUY":"SELL",layers=s.layers||{};return`<section class="gms2 ${go?buy?"buy":"sell":"blocked"}"><span class="eyebrow">${name}-SIDE</span><h2>${go?`${name} LIMIT CANDIDATE`:`NO ${name} LIMIT SIGNAL`}</h2><p>${go?"All strict live gates currently pass. Re-check immediately before placing any pending order.":"The engine is working but one or more safety/evidence gates are blocking this side."}</p><div class="gms2-grid"><div><span>XAUUSD</span><strong>${val(s.currentPrice)}</strong></div><div><span>Conviction</span><strong>${n(s.confidenceScore)??0}/100</strong></div><div><span>Macro score</span><strong>${n(s.macroScore)===null?"—":`${n(s.macroScore)>=0?"+":""}${n(s.macroScore).toFixed(2)}`}</strong></div><div><span>Agreement</span><strong>${n(s.macroAgreementPct)===null?"—":`${Math.round(n(s.macroAgreementPct))}%`}</strong></div></div>${go?`<div class="gms2-plan"><div><span>Limit</span><strong>${val(s.limitPrice)}</strong></div><div><span>Stop ref.</span><strong>${val(s.stopLossReference)}</strong></div><div><span>TP1</span><strong>${val(s.tp1Reference)}</strong><small>${val(s.rrTp1)}R</small></div><div><span>TP2</span><strong>${val(s.tp2Reference)}</strong><small>${val(s.rrTp2)}R</small></div></div>`:""}<div class="gms2-layers"><span>48h ${val(layers.fresh)}</span><span>7d ${val(layers.weekly)}</span><span>90d ${val(layers.regime)}</span><span>Market ${val(layers.market)}</span></div>${s.blockers.length?`<div class="gms2-block"><strong>Blocked because</strong><ul>${s.blockers.map(x=>`<li>${esc(x)}</li>`).join("")}</ul></div>`:""}</section>`}
function renderError(message){install();const p=document.getElementById("signalsPanel");if(!p)return;p.innerHTML=`<section class="gms2 blocked"><span class="eyebrow">SIGNALS BACKEND</span><h2>Signals unavailable</h2><p>${esc(message||"The backend did not return a valid Signals payload.")}</p><div class="gms2-block"><strong>What this means</strong><ul><li>Gold Mine will not invent a BUY or SELL signal.</li><li>Tap Refresh signals live to retry the Worker directly.</li><li>Check Data & methodology for whether the app is on TRUE LIVE or backup.</li></ul></div></section>`}
function render(){install();const p=document.getElementById("signalsPanel");if(!p||!latest)return;if(!hasSignals(latest)){renderError("The current snapshot does not contain buyLimit/sellLimit calculations.");return}const buy=normalized("buyLimit","buy"),sell=normalized("sellLimit","sell"),next=buy.nextEvent||sell.nextEvent,live=latest?._goldMineSource==="worker"||latest?.collectorMode==="cloudflare-true-live"||latest?.sourceStatus?.worker==="true-live";let nextText="No upcoming medium/high-impact event loaded";if(next?.dateUtc){const mins=Math.max(0,Math.round((Date.parse(next.dateUtc)-Date.now())/60000));nextText=`${esc(next.name||"US event")} · ${fmt(next.dateUtc)} · about ${mins} min away`};p.innerHTML=`<section class="gms2-summary"><strong>${live?"TRUE LIVE":"BACKUP"}</strong><span>OANDA ${ageText(buy.marketAt||sell.marketAt)} · signals ${ageText(buy.generated||sell.generated)}</span></section>${card(buy)}${card(sell)}<section class="gms2-summary"><strong>Next news risk</strong><span>${nextText}</span></section><p class="gms2-note">Planning support only. A candidate is not a guarantee of profit or safety.</p>`}
async function refresh(force){if(busy)return;busy=true;const b=document.getElementById("signalsRefresh");if(b){b.disabled=true;b.textContent=force?"Refreshing Worker…":"Checking…"}try{latest=await fetchData(force);window.GoldMineLiveSnapshot=latest;render();window.dispatchEvent(new CustomEvent("goldmine-signals-updated",{detail:latest}));if(b)b.textContent=latest._goldMineSource==="github-backup"?"Backup loaded":"Live checked ✓"}catch(e){console.warn("Signals refresh failed",e);renderError(e.message);if(b)b.textContent="Refresh failed"}finally{busy=false;if(b){b.disabled=false;setTimeout(()=>b.textContent="Refresh signals live",2200)}}}
function style(){if(document.getElementById("gms2-style"))return;const s=document.createElement("style");s.id="gms2-style";s.textContent=`.gms2,.gms2-summary{background:#fff;border:1px solid #e5e1d8;border-radius:18px;padding:15px;margin-bottom:12px}.gms2{border-left:6px solid #aaa}.gms2.buy{border-left-color:#247a52}.gms2.sell{border-left-color:#b43a3a}.gms2.blocked{border-left-color:#aaa}.gms2 h2{margin:5px 0}.gms2 p,.gms2-note{font-size:11px;color:#666d76;line-height:1.45}.gms2-grid,.gms2-plan{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:12px}.gms2-grid>div,.gms2-plan>div{background:#f7f6f2;border-radius:11px;padding:10px}.gms2-grid span,.gms2-plan span{display:block;font-size:9px;color:#777;text-transform:uppercase}.gms2-grid strong,.gms2-plan strong{display:block;margin-top:4px;font-size:13px}.gms2-plan small{font-size:9px;color:#777}.gms2-layers{display:flex;gap:6px;flex-wrap:wrap;margin-top:10px}.gms2-layers span{font-size:9px;background:#f4f3ef;border-radius:999px;padding:5px 7px}.gms2-block{margin-top:11px;padding:10px;background:#fff9f8;border:1px solid #efd4d1;border-radius:12px}.gms2-block li{font-size:10.5px;line-height:1.4;margin:5px 0}.gms2-summary{display:flex;justify-content:space-between;gap:10px}.gms2-summary span{font-size:10px;color:#70757d;text-align:right}@media(max-width:700px){.gms2-grid,.gms2-plan{grid-template-columns:1fr 1fr}}`;document.head.appendChild(s)}
function boot(){style();install();window.addEventListener("goldmine-snapshot-updated",e=>{if(e.detail){latest=e.detail;render()}});document.addEventListener("visibilitychange",()=>{if(!document.hidden&&active())refresh(false)});setTimeout(()=>refresh(false),400);timer=setInterval(()=>{if(!document.hidden&&active())refresh(false)},15000)}
boot();
})();
