(()=>{
  "use strict";
  const GITHUB="https://raw.githubusercontent.com/umarvandutch/Gold-Mine/main/live-data.json";
  const worker=()=>String(window.GOLD_MINE_CONFIG?.liveWorkerUrl||"").trim();
  const CHECK_KEY="goldmine-last-check-at",SNAPSHOT_KEY="goldmine-last-snapshot-at";
  let busy=false,timer=null;
  const ageMin=v=>{if(!v)return 99999;const n=new Date(v).getTime();return Number.isFinite(n)?Math.max(0,(Date.now()-n)/60000):99999};
  const fmtAge=m=>m<1?"just now":m<60?`${Math.floor(m)} min ago`:`${Math.floor(m/60)}h ${Math.floor(m%60)}m ago`;
  async function fetchJson(url){const r=await fetch(url,{cache:"no-store"});if(!r.ok)throw new Error(`HTTP ${r.status}`);const j=await r.json();if(!Array.isArray(j.events))throw new Error("Invalid snapshot");return j;}
  async function fetchLatest(manual=false){const w=worker();if(w){try{return await fetchJson(`${w}${w.includes("?")?"&":"?"}${manual?"fresh=1&":""}t=${Date.now()}`)}catch(e){console.warn("Live Worker unavailable; using GitHub backup",e)}}return fetchJson(`${GITHUB}?backupOnly=1&t=${Date.now()}`)}
  function ensureRows(){const last=document.getElementById("lastRefresh");if(!last)return;const row=last.closest(".build-info");if(row?.querySelector("span"))row.querySelector("span").textContent="Latest macro/news query";const parent=row?.parentElement;if(!parent)return;if(!document.getElementById("lastAppCheck")){const x=document.createElement("div");x.className="build-info";x.innerHTML='<span>Last app check</span><strong id="lastAppCheck">—</strong>';parent.insertBefore(x,row)}if(!document.getElementById("marketObservedAt")){const x=document.createElement("div");x.className="build-info";x.innerHTML='<span>OANDA market observed</span><strong id="marketObservedAt">—</strong>';parent.insertBefore(x,row?.nextSibling)}}
  function updateUI(data,checkedAt){ensureRows();const macro=data?.sourceQueriedAt||data?.generatedAt,market=data?.technical?.observedAt||data?.market?.xau?.observedAt,ma=ageMin(macro),oa=ageMin(market),usingWorker=data?.sourceStatus?.worker==="true-live"||data?.collectorMode==="cloudflare-true-live";const c=document.getElementById("lastAppCheck"),m=document.getElementById("lastRefresh"),o=document.getElementById("marketObservedAt");if(c)c.textContent=checkedAt?`${new Date(checkedAt).toLocaleTimeString()} · ${usingWorker?"live sources queried":"backup checked"}`:"—";if(m)m.textContent=macro?`${new Date(macro).toLocaleString()} · ${fmtAge(ma)}`:"—";if(o)o.textContent=market?`${new Date(market).toLocaleString()} · ${fmtAge(oa)}`:"—";const title=document.getElementById("dataStatusTitle"),text=document.getElementById("dataStatusText");if(title&&text){if(usingWorker&&oa<=2&&ma<=2){title.textContent="LIVE · sources refreshed now";text.textContent=`OANDA, calendar/news, technicals and Signals were queried ${fmtAge(Math.max(oa,ma))}.`;}else if(oa<=15&&ma<=20){title.textContent="Live data connected";text.textContent=`Latest OANDA ${fmtAge(oa)} · macro/news ${fmtAge(ma)}.`;}else{title.textContent="BACKEND STALE";text.textContent=`Latest OANDA is ${fmtAge(oa)} and macro/news is ${fmtAge(ma)}. Signals remain blocked until fresh data arrives.`;}}}
  function nextDelay(data){const sec=Number(data?.worker?.recommendedClientPollSeconds);return Number.isFinite(sec)?Math.max(5,Math.min(60,sec))*1000:15000;}
  function schedule(data){clearTimeout(timer);timer=setTimeout(async()=>{if(!document.hidden){const d=await check({manual:false});schedule(d||data)}else schedule(data)},nextDelay(data))}
  async function check({manual=false}={}){if(busy)return null;busy=true;const btn=document.getElementById("recalcButton");if(manual&&btn){btn.disabled=true;btn.textContent="Refreshing ALL live sources…"}try{const data=await fetchLatest(manual),checkedAt=new Date().toISOString();sessionStorage.setItem(CHECK_KEY,checkedAt);const stamp=String(data?.sourceQueriedAt||data?.generatedAt||"");if(stamp)sessionStorage.setItem(SNAPSHOT_KEY,stamp);updateUI(data,checkedAt);window.dispatchEvent(new CustomEvent("goldmine-snapshot-updated",{detail:data}));const oa=ageMin(data?.technical?.observedAt||data?.market?.xau?.observedAt),ma=ageMin(data?.sourceQueriedAt||data?.generatedAt),trueLive=(data?.sourceStatus?.worker==="true-live"||data?.collectorMode==="cloudflare-true-live")&&oa<=2&&ma<=2;if(manual&&btn){btn.textContent=trueLive?"All live sources refreshed ✓":oa<=15?"Latest available loaded":"BACKEND STALE";if(trueLive){const active=document.querySelector('.nav-item.active')?.dataset.view||"alerts";sessionStorage.setItem("goldmine-resume-view",active);setTimeout(()=>location.reload(),420)}}return data}catch(e){console.warn("Live refresh failed",e);if(manual&&btn)btn.textContent="Refresh failed — retry";return null}finally{busy=false;if(manual&&btn){btn.disabled=false;setTimeout(()=>{if(!busy)btn.textContent="Refresh all live"},2600)}}}
  function installButton(){const old=document.getElementById("recalcButton");if(!old||old.dataset.liveV4)return;const b=old.cloneNode(true);b.dataset.liveV4="1";b.textContent="Refresh all live";old.replaceWith(b);b.addEventListener("click",()=>check({manual:true}))}
  function restoreView(){const name=sessionStorage.getItem("goldmine-resume-view");if(!name)return;sessionStorage.removeItem("goldmine-resume-view");setTimeout(()=>document.querySelector(`.nav-item[data-view="${name}"]`)?.click(),100)}
  async function boot(){installButton();restoreView();ensureRows();const d=await check();schedule(d);document.addEventListener("visibilitychange",()=>{if(!document.hidden){installButton();check({manual:false}).then(schedule)}});window.addEventListener("focus",()=>{installButton();check({manual:false}).then(schedule)});new MutationObserver(()=>installButton()).observe(document.body,{childList:true,subtree:true})}
  boot();
})();
